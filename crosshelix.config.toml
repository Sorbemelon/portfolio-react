# Maximum text file size CrossHelix may index or fully expand.
max_file_size_kb = 512

# Maximum lines returned by default/full expansion before truncation safeguards apply.
max_expand_lines = 300

# Default deterministic graph-neighbor result count.
neighbors_default_limit = 8

# Default search result count.
search_default_limit = 10
